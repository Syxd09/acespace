const menu=document.querySelector('.page-menu');const nav=document.querySelector('.page-nav');if(menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)})}document.querySelectorAll('[data-reveal]').forEach((el,index)=>{el.style.transitionDelay=`${index*70}ms`;new IntersectionObserver(([entry])=>{if(entry.isIntersecting)entry.target.classList.add('in')},{threshold:.15}).observe(el)});
